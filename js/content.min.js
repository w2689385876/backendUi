/**
 * @param {string} key
 * @return {?}
 */
function $childNode(key) {
  return window.frames[key];
}
/**
 * @param {Object} input
 * @param {string} i
 * @return {undefined}
 */
function animationHover(input, i) {
  input = $(input);
  input.hover(function() {
    input.addClass("animated " + i);
  }, function() {
    window.setTimeout(function() {
      input.removeClass("animated " + i);
    }, 2E3);
  });
}
/**
 * @return {undefined}
 */
function WinMove() {
  /** @type {string} */
  var parentSelector = "[class*=col]";
  /** @type {string} */
  var handle = ".ibox-title";
  /** @type {string} */
  var regions = "[class*=col]";
  $(parentSelector).sortable({
    handle : handle,
    connectWith : regions,
    tolerance : "pointer",
    forcePlaceholderSize : true,
    opacity : 0.8
  }).disableSelection();
}
/** @type {Document} */
var $parentNode = window.parent.document;
if ($(".tooltip-demo").tooltip({
  selector : "[data-toggle=tooltip]",
  container : "body"
}), $(".modal").appendTo("body"), $("[data-toggle=popover]").popover(), $(".collapse-link").click(function() {
  var container = $(this).closest("div.ibox");
  var code = $(this).find("i");
  var $accordion = container.find("div.ibox-content");
  $accordion.slideToggle(200);
  code.toggleClass("fa-chevron-up").toggleClass("fa-chevron-down");
  container.toggleClass("").toggleClass("border-bottom");
  setTimeout(function() {
    container.resize();
    container.find("[id^=map-]").resize();
  }, 50);
}), $(".close-link").click(function() {
  var selfObj = $(this).closest("div.ibox");
  selfObj.remove();
}), top == this) {
  /** @type {string} */
  var gohome = '<div class="gohome"><a class="animated bounceInUp" href="index.html?v=3.0" title="\u8fd4\u56de\u9996\u9875"><i class="fa fa-home"></i></a></div>';
  $("body").append(gohome);
};